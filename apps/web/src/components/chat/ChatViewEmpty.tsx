/**
 * Empty state rendered when no activeThread is present.
 * Re-exports from ChatViewEmptyState to avoid duplication.
 */
export { ChatViewEmptyState as ChatViewEmpty } from './ChatViewEmptyState'
